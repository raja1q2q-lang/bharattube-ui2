# BharatTube frontend → deployed backend (Node + Express + MongoDB)
# This is the ONLY variable the frontend needs. No DATABASE_URL / PostgreSQL.
NEXT_PUBLIC_API_URL=https://bharattube-ylmq.onrender.com/api/v1
